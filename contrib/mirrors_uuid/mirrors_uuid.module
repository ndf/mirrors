<?php

/**
 * @file
 * Module file for mirrors_uuid module.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function mirrors_uuid_ctools_plugin_directory($module, $plugin) {
  if ($module == 'mirrors') {
    return 'plugins/' . $plugin;
  }
}
